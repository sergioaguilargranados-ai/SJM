import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { modulos_sistema, funciones_sistema, acciones_sistema } from "@/lib/schema"; import { eq } from "drizzle-orm"; export async function GET() { try { const modules = await db.select().from(modulos_sistema); const functions = await db.select().from(funciones_sistema); const actions = await db.select().from(acciones_sistema); return NextResponse.json({ modules, functions, actions }); } catch (error: any) { return NextResponse.json({ error: error.message }); } }
